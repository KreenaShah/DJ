def encrypt(pt, key):
    columns = len(key)
    
    pass

def decrypt(ciphertext, key):
    pass

pt = "wearediscoveredfleeatonce"
key = "zebras"

encryptedText = encrypt(pt, key)
print("Ciphertext:", encryptedText)
decryptedText = decrypt(encryptedText, key)
print("Decrypted text:", decryptedText)